import React, { JSX } from 'react'

export const Projects: React.FC<{ scrollHandler: (scrollTo: string) => () => void }> = ({ scrollHandler }): JSX.Element => (
	<>
		<span className='screen-title'>🚧 Project repository 🚧</span>
		<br />
		<br />
		<div>...🚧under construction👷🏻‍♀️...</div>
	</>
)
